# GeoScope — プロダクト要求定義書 (PRD)

**Version**: 0.4.0
**Date**: 2026-03-08
**Status**: v0.3 完了 — v0.4 計画中

---

## 1. プロダクト概要

### 1.1 プロダクト名

**GeoScope**

### 1.2 ビジョン

「開いたら見える、触れる、コードになる」

地球惑星流体データを、専門知識やスクリプト記述なしに即座に可視化・探索でき、
GUI操作がそのまま再現可能なコードとして記録されるデスクトップアプリケーション。

### 1.3 ポジショニング

GFD（地球流体力学）研究における可視化ツールの世代交代を目指す。
GrADS・NCL・Panoply が担ってきた役割を、現代的な UX で再定義する。

### 1.4 dcmodel との関係

dcmodel ファミリー（ispack-rs, spmodel-rs 等）の出力可視化を主要ユースケースとするが、
CF Conventions 準拠の NetCDF データ全般を対象とする汎用ツールとして設計する。
fludarium（流体可視化デモ）とは独立したプロダクトとする。

### 1.5 fludarium との棲み分け

| | **fludarium** | **GeoScope** |
|---|---|---|
| 用途 | リアルタイムシミュレーション可視化 | ポスト処理・解析可視化 |
| 入力 | パイプ (`--stream`), ライブデータ | ファイル (NetCDF) |
| 対象 | デモ、にぎやかし | 研究・教育 |
| UI | ミニマル（キーバインド中心） | フルGUI（パネル、Inspector 等） |

fludarium の球面レンダリング・カラーマップ等の実装は GeoScope に移植・再利用する。

---

## 2. ターゲットユーザーとペルソナ

### 2.1 優先ペルソナ

#### ペルソナ 1: 大学院生「田中さん」(M1)

- **背景**: 学部は物理。Python は少し書ける。GrADS/NCL は触ったことない
- **動機**: 指導教員から「まず浅水方程式の出力を見てみて」と言われた
- **痛点**: 環境構築に半日、先輩のスクリプトをコピペ改変、理解なき利用
- **ゴール**: 5分以内に自分のデータを地球上で回せること
- **成功指標**: ファイルを開いてから最初の描画まで 30 秒以内

#### ペルソナ 2: 教員「佐藤先生」

- **背景**: 20年来の GrADS 使い。授業で大気力学を教えている
- **動機**: 学生に現象を「見せたい」が、準備に毎回時間がかかる
- **痛点**: ライブデモの準備コスト、学生への環境配布の難しさ
- **ゴール**: 授業中にリアルタイムで操作し、操作を「レシピ」として学生に配布
- **成功指標**: 授業準備が従来の 1/3 以下の時間で完了

### 2.2 将来のペルソナ（v1 以降）

- 研究者（解析フェーズ）: 複数実験の比較、パラメータ感度解析
- 研究者（論文執筆フェーズ）: publication-quality の図の出力
- 大規模シミュレーション運用者: TB 級データのサーバーサイド描画

---

## 3. デザイン原則

| # | 原則 | 説明 |
|---|------|------|
| 1 | **Zero-config first view** | ファイルを開いたら 0 秒で「賢い初期描画」を表示する |
| 2 | **Direct manipulation** | データを「触って」探索する。地球を回す、断面を引く |
| 3 | **Progressive disclosure** | 初学者には簡潔に、上級者には全機能を段階的に開示する |
| 4 | **Code is a first-class artifact** | GUI 操作が常にコードとして記録・再現可能 |
| 5 | **Teach, don't hide** | 操作の裏で何が起きているか学べるヒントを提示する |

---

## 4. 機能要件

### 4.1 ファイル読み込みと自動描画

**概要**: NetCDF ファイルを開いた瞬間に、メタデータから最適な描画法を自動推論して表示する。

**自動推論ロジック（3段階フォールバック）**:

> **重要**: dcmodel (gtool-rs) の出力はフィールド変数に CF `standard_name` を持たない。
> Level 1 だけに依存せず、3段階フォールバックを必須とする。

**Level 1 — CF standard_name（確信度: 高）**:
- CF Conventions の `standard_name` / `units` から変数の種類を判定
- 該当すれば最適な描画法・カラーマップを自動選択

**Level 2 — ヒューリスティクス（確信度: 中）**:
- 変数名パターンマッチ: `vor*` → 渦度, `temp*`/`T` → 温度, `d*` → 偏差(diverging)
- `long_name` 属性からの推論
- `units` 属性: `K` → 温度, `m/s` → 速度, `Pa` → 気圧
- gtool-rs 独自属性の活用: `grid_type`, `truncation_nm`, `model`, `params`
- 座標変数名の柔軟認識: `lon`/`longitude`/`x`/`g0_lon_1`/`nav_lon` 等

**Level 3 — 次元構造のみ（確信度: 低、最終フォールバック）**:
- 2D (`dim1 x dim2`) → カラーフィル + viridis
- 1D → ラインプロット
- データ統計による diverging/sequential 自動判定: `min * max < 0` → diverging (RdBu_r), else → sequential (viridis)

**描画法の自動選択**:
- 座標軸 (`lon`, `lat`, `lev`, `time`) を自動認識
- 2D (`lon x lat`) → Globe view + カラーフィル
- 3D (`lon x lat x lev`) → Globe view（地表レベル）+ 鉛直スライダー追加
- 時系列あり → タイムスライダー追加

**カラーマップ自動選択**:

| 変数 | カラーマップ | タイプ |
|------|------------|--------|
| 温度（絶対値） | inferno / thermal | sequential（常に正） |
| 温度偏差 / アノマリ | RdBu_r | diverging, 0中心 |
| 渦度 | RdBu_r | diverging, 0中心 |
| 発散 D | RdBu_r | diverging, 0中心 |
| 帯状風 u | RdBu_r | diverging, 0中心（東風-/西風+） |
| 降水量 | YlGnBu | sequential |
| ジオポテンシャル | viridis | sequential |
| 地形 | terrain | sequential |
| `d*` プレフィックス (dvort, dphi) | RdBu_r | diverging（自動偏差検出） |
| 不明 + min*max < 0 | RdBu_r | diverging |
| 不明 + min*max >= 0 | viridis | sequential |

**推論結果の透明性**（原則 5: Teach, don't hide）:
- Inspector に「"渦度" と推論しました（変数名 `vor` から判定）」のようなヒントを表示
- ステータスバーに推論結果を常時表示
- ワンクリックで変数タイプを修正可能

**入力方法**:
- ドラッグ & ドロップ
- GUI ファイルダイアログ（+ ボタン）
- コマンドラインからの起動 (`geoscope file.nc`)

### 4.2 球面操作（Direct Manipulation）

**マウス操作**（Google Earth / Cesium 標準に準拠）:

| 操作 | 動作 |
|------|------|
| 左ドラッグ | 地球回転 |
| 右ドラッグ | パン（視点移動） |
| スクロール | ズーム（領域拡大） |
| ホバー | ポイント情報（緯度・経度・値） |
| Shift+クリック | ピンを立てる（複数点の比較） |

**キーボードショートカット**:

| キー | 動作 |
|------|------|
| Space | 時間アニメーション再生/停止 |
| ← → | 時間ステップ移動 |
| ↑ ↓ | 鉛直レベル移動 |
| 1-6 | ビュー切替 |
| Cmd+E | PNG エクスポート |

### 4.3 ビュータイプ

| ビュー | 説明 | 状態 |
|--------|------|------|
| **Globe** | 正射影の 3D 地球儀。回転・ズーム・グロー | ✅ v0.1b |
| **Map** | 2D 地図投影。Equirectangular / Mollweide | ✅ v0.2 |
| **Hovmoller** | ホフメラー図（時間-経度ヒートマップ） | ✅ v0.1b |
| **Spectrum** | E(n) エネルギースペクトル図（log-log） | ✅ v0.1b |
| **Section** | 鉛直断面図（Fix Lat / Fix Lon） | ✅ v0.2 |
| **Profile** | 特定地点の鉛直プロファイル / 時系列 | 🔲 v0.3 |

**Map ビューの投影法**:

| 投影法 | 状態 |
|--------|------|
| Equirectangular (Plate Carree) | ✅ v0.1b |
| Mollweide | ✅ v0.2 |
| Polar Stereographic | 🔲 v0.3 |
| Robinson | 🔲 将来 |

### 4.4 ベクトル場の重ね合わせ

| 描画法 | 説明 | 状態 |
|--------|------|------|
| Wind arrows | 等間隔の矢印。Globe/Map 両対応 | ✅ v0.2 |
| Streamlines | 流線。流れの全体像を把握 | 🔲 v0.3 |
| Wind barbs | 気象学標準の記法 | 🔲 将来 |

### 4.5 Inspector パネル

GUI で調整可能な描画パラメータ:

- [x] 変数選択（ComboBox）
- [x] 投影法選択（ComboBox、全ビュー表示）
- [x] カラーマップ選択（グラデーションプレビュー + 説明ラベル）
- [x] 値範囲（Slice/Global/Manual + Symmetric 0-centered）
- [x] 表示モード（Grid/Smooth トグル）
- [x] ベクトルオーバーレイ（On/Off, u/v 選択, Density/Scale）
- [x] 鉛直断面（Fix Lat/Fix Lon, Index スライダー）
- [x] 推論結果表示（カテゴリ、信頼度）
- [x] PNG エクスポート（解像度 1x/2x/4x, カラーバー, タイトル）
- [ ] ポイント情報（カーソル位置の座標・値）

### 4.6 コード双方向同期

**同期方式: GUI マスター方式**

Code Panel は「現在の GUI 状態のスナップショット」を常に反映する。

- GUI 操作 → Code Panel が自動更新
- ユーザーがコードを編集中 → 自動更新を一時停止
- 「実行」ボタン押下 → コードが GUI 状態を上書き

**生成コードフォーマット**: Python (xarray / matplotlib 互換)

```python
# GeoScope auto-generated
import geoscope as gs

ds = gs.open("exp01.nc")
fig = ds.vorticity.plot(
    time="48h",
    projection="orthographic",
    center=(135, 35),
    cmap="RdBu_r",
    symmetric=True,
)
fig.save("vorticity_48h.png")
```

**レシピ保存**:
- 操作をスクリプトファイル (.py) として保存
- `geoscope run recipe.py` で同じ図を再現

### 4.7 データブラウザ

- [x] ファイル変数をツリー表示
- [x] 変数のメタデータ（単位、次元、サイズ）をツールチップ表示
- [x] 変数クリック → 即座に描画
- [x] 複数ファイルを同時に開ける（+ ボタン / D&D）
- [x] ファイル閉じる（× ボタン）
- [x] 変数タイプ別ドット色分け（赤=diverging/緑=scalar/橙=velocity）

### 4.8 LLM Copilot（v0.5 以降）

**概要**: LLM による対話的支援。可視化の物理的意味の解説、自然言語による操作指示。

| 機能 | 説明 | 優先度 |
|------|------|--------|
| チャットサイドバー | 対話的な質問・解説 | v0.5 |
| コマンドパレット | Cmd+K → 自然言語 → 操作 | v0.5 |
| Explain モード | 現在のビューの物理的解説 | v0.5 |
| Suggest モード | データ駆動の分析提案 | v0.6 |

---

## 5. 画面構成

```
+----------+--------------------------------------+---------------+
|          |                                      |               |
| Data     |         Main Viewport                |  Inspector    |
| Browser  |                                      |               |
|          |    +-------------------------+       |  Variable:    |
| [exp01]  |    |                         |       |  [vorticity]  |
|  ● vor   |    |      Globe View         |       |               |
|  ● div   |    |                         |       |  Projection:  |
|  ● temp  |    |   (drag to rotate)      |       |  [Orthographic|
|  ● ps    |    |                         |       |            ]  |
| [exp02]  |    |                         |       |               |
|  ● vor   |    +-------------------------+       |  Colormap:    |
|  ● ...   |                                      |  [RdBu_r    ] |
|          |  💡 Detected: vorticity (RdBu_r, 0c) |               |
|    +     |                                      |  Range:       |
|          |  [🌐 Globe][Map][Hov][Spec][Sect]     |  [-5e-5] to   |
|          |                                      |  [5e-5]       |
|          |  ▶ t=48  ═══════O══════════ 720       |  ☑ Symmetric  |
+----------+--------------------------------------+               |
                                                  |  Point Info:  |
                                                  |  lon=135.0°E  |
                                                  |  lat= 35.0°N  |
                                                  |  val= 2.3e-5  |
                                                  +---------------+
```

---

## 6. 既存ツールとの差別化

v0.2 時点での現実的な評価（◎=卓越 ○=良好 △=限定的 ✕=非対応）:

|                     | GrADS | Panoply | ParaView | Matplotlib | **GeoScope v0.2** | **GeoScope 将来** |
|---------------------|:-----:|:-------:|:--------:|:----------:|:-----------------:|:-----------------:|
| Zero-config 描画    |   △   |    ○    |    ✕     |     ✕      |       **◎**       |      **◎**       |
| 球面直接操作        |   ✕   |    △    |    ○     |     ✕      |       **◎**       |      **◎**       |
| 探索的解析          |   △   |    △    |    ○     |     △      |       **○**       |      **◎**       |
| コード生成          |   ✕   |    ✕    |    △     |     -      |         ✕         |      **◎**       |
| Publication 品質    |   ○   |    △    |    ○     |     ◎      |         △         |        ○         |
| 学習コスト(低=良)    |   ✕   |    ○    |    △     |     △      |       **◎**       |      **◎**       |
| GFD ドメイン特化    |   ○   |    △    |    ✕     |     △      |       **◎**       |      **◎**       |
| スペクトル法連携    |   ✕   |    ✕    |    ✕     |     △      |       **○**       |      **◎**       |
| LLM 支援           |   ✕   |    ✕    |    ✕     |     ✕      |         ✕         |      **◎**       |

---

## 7. 技術スタック

| レイヤー | 技術選定 | バージョン |
|---------|---------|-----------|
| GUI フレームワーク | eframe + egui_dock | 0.33 / 0.18 |
| 3D レンダリング | wgpu (via eframe) | 27 |
| NetCDF 読み込み | netcdf-rs | 0.12 |
| PNG エクスポート | image | 0.25 |
| ファイルダイアログ | rfd | 0.15 |
| HDF5 | hdf5 (Homebrew) | 1.10 |
| 地図投影 | 自前実装 (Newton 法 Mollweide 等) | — |

### Globe 描画アーキテクチャ

```
[NetCDF data] → [CPU: 2D array] → [GPU: 2D Texture R32Float]
                                          |
[Colormap LUT] → [GPU: 1D Texture] → [Fragment Shader: lookup]
                                          |
                                   [UV Sphere Mesh → Screen]
```

- カラーマップ変更 = 1D テクスチャ差し替えのみ（LUT 全10種を起動時プリロード）
- データ更新 = 2D テクスチャ転送のみ
- Grid/Smooth 切替 = シェーダー内バイリニア補間フラグ

---

## 8. スコープ定義

### v0.1a — 技術検証 ✅ Done

目標: 「spmodel-rs の NetCDF 出力を D&D で Globe 表示できる」

- [x] wgpu + egui (egui_dock) のスケルトン UI
- [x] NetCDF ファイル D&D → Globe ビュー（回転・ズーム）
- [x] fludarium レンダラーの移植（テクスチャ方式）
- [x] カラーマップ (viridis, RdBu_r)
- [x] **ピボット判断**: egui + wgpu で続行を決定（Tauri ピボットしない）

### v0.1b — 使える最小形 ✅ Done

目標: 「Panoply より快適に dcmodel 出力を見られる」

- [x] Data Browser（変数ツリー、クリック選択）
- [x] Inspector（カラーマップ、値範囲）
- [x] タイムスライダー（再生/一時停止、速度制御）
- [x] Map ビュー（Equirectangular、pan+zoom）
- [x] 自動推論（3段階フォールバック）
- [x] ホフメラー図（時間-経度ヒートマップ）
- [x] E(n) エネルギースペクトル図（log-log）
- [x] PNG エクスポート
- [x] ダークテーマ UI

### v0.2 — GFD 研究者必須機能 ✅ Done

目標: 「研究に使える可視化ツール」

**P1 — 投影・断面・ベクトル**:
- [x] Mollweide 投影（Newton 法 θ 求解）
- [x] 鉛直断面ビュー（Fix Lat / Fix Lon 切替）
- [x] ベクトル場オーバーレイ（u/v 自動検出、Globe/Map 両対応、矢印描画）

**P2 — 操作性・品質**:
- [x] Level 選択スライダー
- [x] カラーマップレンジ制御（Slice / Global / Manual）
- [x] UI ファイルオープン（+ ボタン、rfd）
- [x] マルチファイル切替
- [x] カラーマップ刷新（10種、9ストップ高精度、カテゴリ付き UI）
- [x] エクスポート UI ダイアログ（解像度 1x/2x/4x、カラーバー、タイトル）
- [x] LUT キャッシュ（全10種を起動時プリロード）
- [x] Vector overlay 座標キャッシュ
- [x] デザインシステム刷新（デザイントークン、モック準拠 UI）

### v0.3 — インタラクション強化 ✅ Done

目標: 「触って理解できる」— Direct Manipulation の完成

**P1 — 必須** ✅:
- [x] Point Info（Map ホバーで緯度・経度・値をビューポート左下に表示）
- [x] Profile ビュー（鉛直プロファイル / 時系列グラフ、ホバークロスヘア、プレイヘッドマーカー）
- [x] キーボードショートカット（Space=再生, ←→=時間, ↑↓=レベル, 1-6=ビュー, G/C/V=トグル）

**P2 — 高優先** ✅:
- [x] Polar Stereographic 投影（北極/南極、円盤メッシュ）
- [x] ストリームライン描画（RK4 積分、バイリニア補間、矢じり付き）
- [x] Zonal Mean 計算（DataStore::compute_zonal_mean）
- [x] 等値線オーバーレイ（Marching Squares、ラベル、Globe/Map 対応）

**P3 — 望ましい**:
- [ ] アニメーション GIF / MP4 出力
- [ ] Publication-quality エクスポート（フォント埋め込み、軸ラベル）
- [ ] 波数フィルタスライダー（スペクトル法教育機能）

### v0.4 — アノテーション + コード双方向同期

目標: 「外部解析結果を重ねて見せる」+「GUI 操作がそのままコードになる」

- [ ] **アノテーションレイヤー**（JSON/CSV → Globe/Map オーバーレイ）
  - 軌跡 (trajectory): (time, lon, lat) の折れ線 — 渦中心追跡、台風経路、浮子軌跡等
  - マーカー (marker): 観測点、特徴点
  - 線分 (line): 断面線、境界線
  - D&D または File Open で読み込み、時間同期でアニメーション
- [ ] Code Panel（GUI → Python テキスト生成）
- [ ] コード → GUI 反映（GUI マスター方式）
- [ ] レシピ保存・読み込み（.py ファイル）
- [ ] Rhai スクリプトエンジン統合

### v0.5 — LLM Copilot

目標: 「AI と対話しながら探索する」

- [ ] チャットサイドバー（Explain + Explore モード）
- [ ] コマンドパレット（Cmd+K → 自然言語 → 操作）
- [ ] ドメイン知識コンテキスト（GFD 基礎 + スペクトル法）
- [ ] ハイブリッド実行（クラウド API + ローカル LLM）

### v0.6 — 比較・高度解析

- [ ] 比較モード（並列ビュー、差分ビュー、同期回転）
- [ ] 保存量診断ミニパネル（質量・エネルギー・エンストロフィー）
- [ ] 質量流線関数の計算・表示
- [ ] LLM Suggest モード（データ駆動の自動分析提案）

### 将来

- [ ] ブラウザ版 (WebAssembly + WebGPU)
- [ ] サーバーサイドレンダリング（大規模データ対応）
- [ ] プラグインシステム
- [ ] EP フラックス断面図
- [ ] 球面調和関数モード表示（Y^m_n）
- [ ] 波数-周波数スペクトル

---

## 9. 非機能要件

| 項目 | 要件 | 現状 |
|------|------|------|
| 初期描画速度 | ファイルオープンから描画まで 1 秒以内 | ✅ 達成 |
| フレームレート | 球面回転操作中 60fps | ✅ 達成 |
| メモリ使用量 | 1GB 以下のファイルはメモリ内で処理 | ✅ 達成 |
| 対応 OS | macOS (ARM), Linux, Windows | macOS のみ |
| アクセシビリティ | キーボード操作で基本機能 | 🔲 v0.3 |

---

## 10. 成功指標

### 定量指標

| 指標 | 目標 |
|------|------|
| 初回描画到達時間 | 30 秒以内 |
| GitHub Stars | 6ヶ月で 100+ |
| ダウンロード数 | 月間 50+ |

### 定性指標

| 指標 | 目標 |
|------|------|
| 電脳倶楽部セミナー後の反応 | 「使いたい」70%+ |
| 研究室導入数 | 初年度 3研究室+ |

---

## 付録 A: 既存ツールの課題分析

### A.1 「コードを書かないと何も見えない」問題

GrADS, NCL, Matplotlib 系はすべて「まずスクリプトを書く」前提。
データを開いて「ざっと見る」だけで 10〜20 行のコードが必要。

### A.2 「時代が止まった GUI」

Panoply, GrADS の GUI は 2000 年代の Java Swing / Tcl/Tk 感覚。
高 DPI ディスプレイ非対応、D&D なし、ダークモードなし。

### A.3 「球面データ特有の操作」が貧弱

地球を回転させながらデータを見る操作が直感的でない。
断面図の切り替えが手間。アニメーションのシークバーがない or 貧弱。

### A.4 「比較」が難しい

2 つの実験結果を並べて見るのが面倒。差分のインタラクティブ表示がない。

---

## 付録 B: 参考ツール一覧

### 研究コミュニティの定番

- **GrADS**: COLA/IGES, 1988年〜。CLI + スクリプト
- **Panoply**: NASA GISS。GUI (Java/Swing)、NetCDF/HDF 簡易閲覧
- **ParaView**: Kitware/Sandia。GUI (Qt)、CFD 全般
- **VAPOR**: NCAR。GUI (Qt + OpenGL)、3D 大気・海洋データ

### Python エコシステム

- **Matplotlib + Cartopy**: 2D 地図投影の定番
- **xarray + hvPlot**: NetCDF 直接扱い + インタラクティブ

### GFD 電脳倶楽部系

- **ISPACK / SPMODEL**: スペクトル法ライブラリ + モデル群
- **gtool / DCL**: データ I/O + Fortran 描画
