# GeoScope — プロダクト要求定義書 (PRD)

**Version**: 0.3.0 (LLM Copilot Added)
**Date**: 2026-03-07
**Status**: 議論中 — 設計レビュー反映済み

---

## 1. プロダクト概要

### 1.1 プロダクト名

**GeoScope**（仮称）

### 1.2 ビジョン

「開いたら見える、触れる、コードになる」

地球惑星流体データを、専門知識やスクリプト記述なしに即座に可視化・探索でき、
GUI操作がそのまま再現可能なコードとして記録されるデスクトップアプリケーション。

### 1.3 ポジショニング

GFD（地球流体力学）研究における可視化ツールの世代交代を目指す。
GrADS・NCL・Panoply が担ってきた役割を、現代的な UX で再定義する。

### 1.4 dcmodel との関係

dcmodel ファミリー（ispack-rs, spmodel-rs 等）の出力可視化を主要ユースケースとするが、
CF Conventions 準拠の NetCDF データ全般を対象とする汎用ツールとして設計する。
fludarium（流体可視化デモ）とは独立したプロダクトとする。

### 1.5 fludarium との棲み分け

| | **fludarium** | **GeoScope** |
|---|---|---|
| 用途 | リアルタイムシミュレーション可視化 | ポスト処理・解析可視化 |
| 入力 | パイプ (`--stream`), ライブデータ | ファイル (NetCDF) |
| 対象 | デモ、にぎやかし | 研究・教育 |
| UI | ミニマル（キーバインド中心） | フルGUI（パネル、Inspector 等） |

fludarium の球面レンダリング・カラーマップ等の実装は GeoScope に移植・再利用する。

---

## 2. ターゲットユーザーとペルソナ

### 2.1 優先ペルソナ

#### ペルソナ 1: 大学院生「田中さん」(M1)

- **背景**: 学部は物理。Python は少し書ける。GrADS/NCL は触ったことない
- **動機**: 指導教員から「まず浅水方程式の出力を見てみて」と言われた
- **痛点**: 環境構築に半日、先輩のスクリプトをコピペ改変、理解なき利用
- **ゴール**: 5分以内に自分のデータを地球上で回せること
- **成功指標**: ファイルを開いてから最初の描画まで 30 秒以内

#### ペルソナ 2: 教員「佐藤先生」

- **背景**: 20年来の GrADS 使い。授業で大気力学を教えている
- **動機**: 学生に現象を「見せたい」が、準備に毎回時間がかかる
- **痛点**: ライブデモの準備コスト、学生への環境配布の難しさ
- **ゴール**: 授業中にリアルタイムで操作し、操作を「レシピ」として学生に配布
- **成功指標**: 授業準備が従来の 1/3 以下の時間で完了

### 2.2 将来のペルソナ（v1 以降）

- 研究者（解析フェーズ）: 複数実験の比較、パラメータ感度解析
- 研究者（論文執筆フェーズ）: publication-quality の図の出力
- 大規模シミュレーション運用者: TB 級データのサーバーサイド描画

---

## 3. デザイン原則

| # | 原則 | 説明 |
|---|------|------|
| 1 | **Zero-config first view** | ファイルを開いたら 0 秒で「賢い初期描画」を表示する |
| 2 | **Direct manipulation** | データを「触って」探索する。地球を回す、断面を引く |
| 3 | **Progressive disclosure** | 初学者には簡潔に、上級者には全機能を段階的に開示する |
| 4 | **Code is a first-class artifact** | GUI 操作が常にコードとして記録・再現可能 |
| 5 | **Teach, don't hide** | 操作の裏で何が起きているか学べるヒントを提示する |

---

## 4. 機能要件

### 4.1 ファイル読み込みと自動描画

**概要**: NetCDF ファイルを開いた瞬間に、メタデータから最適な描画法を自動推論して表示する。

**自動推論ロジック（3段階フォールバック）**:

> **重要**: dcmodel (gtool-rs) の出力はフィールド変数に CF `standard_name` を持たない。
> Level 1 だけに依存せず、3段階フォールバックを必須とする。

**Level 1 — CF standard_name（確信度: 高）**:
- CF Conventions の `standard_name` / `units` から変数の種類を判定
- 該当すれば最適な描画法・カラーマップを自動選択

**Level 2 — ヒューリスティクス（確信度: 中）**:
- 変数名パターンマッチ: `vor*` → 渦度, `temp*`/`T` → 温度, `d*` → 偏差(diverging)
- `long_name` 属性からの推論
- `units` 属性: `K` → 温度, `m/s` → 速度, `Pa` → 気圧
- gtool-rs 独自属性の活用: `grid_type`, `truncation_nm`, `model`, `params`
- 座標変数名の柔軟認識: `lon`/`longitude`/`x`/`g0_lon_1`/`nav_lon` 等

**Level 3 — 次元構造のみ（確信度: 低、最終フォールバック）**:
- 2D (`dim1 x dim2`) → カラーフィル + viridis
- 1D → ラインプロット
- データ統計による diverging/sequential 自動判定: `min * max < 0` → diverging (RdBu_r), else → sequential (viridis)

**描画法の自動選択**:
- 座標軸 (`lon`, `lat`, `lev`, `time`) を自動認識
- 2D (`lon x lat`) → Globe view + カラーフィル
- 3D (`lon x lat x lev`) → Globe view（地表レベル）+ 鉛直スライダー追加
- 時系列あり → タイムスライダー追加

**カラーマップ自動選択**:

| 変数 | カラーマップ | タイプ |
|------|------------|--------|
| 温度（絶対値） | inferno / thermal | sequential（常に正） |
| 温度偏差 / アノマリ | RdBu_r | diverging, 0中心 |
| 渦度 | RdBu_r | diverging, 0中心 |
| 発散 D | RdBu_r | diverging, 0中心 |
| 帯状風 u | RdBu_r | diverging, 0中心（東風-/西風+） |
| 降水量 | YlGnBu | sequential |
| ジオポテンシャル | viridis | sequential |
| 地形 | terrain | sequential |
| `d*` プレフィックス (dvort, dphi) | RdBu_r | diverging（自動偏差検出） |
| 不明 + min*max < 0 | RdBu_r | diverging |
| 不明 + min*max >= 0 | viridis | sequential |

**推論結果の透明性**（原則 5: Teach, don't hide）:
- Inspector に「"渦度" と推論しました（変数名 `vor` から判定）」のようなヒントを表示
- ワンクリックで変数タイプを修正可能
- 学習機能: ユーザーが修正したパターンを記憶し、次回以降に適用

**入力方法**:
- ドラッグ & ドロップ
- メニューからファイル選択
- ファイル関連付けによる起動
- コマンドラインからの起動 (`geoscope open file.nc`)

### 4.2 球面操作（Direct Manipulation）

**マウス操作**（Google Earth / Cesium 標準に準拠）:

| 操作 | 動作 |
|------|------|
| 左ドラッグ | 地球回転 |
| 右ドラッグ | パン（視点移動） |
| スクロール | ズーム（領域拡大、自動投影法遷移） |
| クリック | データ依存: 時間軸あり→時系列、なし→値表示、鉛直軸あり→プロファイル |
| Shift+クリック | ピンを立てる（複数点の比較） |

**断面操作（モード切替方式）**:
- ツールバーのハサミアイコンをクリック → 断面モードに入る
- 断面モード中: 左ドラッグで断面線を引く → Section ビューが自動で開く
- `S` キー押下中にドラッグでも断面モードに入れる（上級者向け）

**トラックパッド / タッチジェスチャー** (macOS):

| 操作 | 動作 |
|------|------|
| 1本指ドラッグ | 地球回転 |
| 2本指スクロール | ズーム |
| ピンチ | ズーム |

**キーボードショートカット**:

| キー | 動作 |
|------|------|
| Space | 時間アニメーション再生/停止 |
| ← → | 時間ステップ移動 |
| 1, 2, 3, 4, 5, 6 | ビュー切替（Globe / Map / Section / Profile / Hovmoller / Spectrum） |
| S + ドラッグ | 断面モード |
| Cmd+Z / Ctrl+Z | アンドゥ |

### 4.3 ビュータイプ

| ビュー | 説明 | 優先度 |
|--------|------|--------|
| **Globe** | 正射影の 3D 地球儀。回転・ズーム可能 | P0 |
| **Map** | 2D 地図投影。デフォルト: Equirectangular（GrADS 標準） | P0 |
| **Hovmoller** | ホフメラー図（時間-経度 / 時間-緯度）。波の伝播方向・速度を一目で把握 | P0 |
| **Spectrum** | E(n) エネルギースペクトル図（1D log-log）。n^{-3} 参照線付き | P0 |
| **Section** | 鉛直断面図（経度固定 / 緯度固定 / 任意断面）。Zonal Mean トグル付き | P1 |
| **Profile** | 特定地点の鉛直プロファイル / 時系列 | P1 |

**Map ビューの投影法**:

| 投影法 | 説明 | 優先度 |
|--------|------|--------|
| Equirectangular (Plate Carree) | **デフォルト**。GrADS ユーザーの移行パス | P0 |
| Mollweide | 全球の面積保存表示。論文でよく使われる | P0 |
| Robinson | 全球表示。見た目のバランスが良い | P1 |
| Polar Stereographic | 極域研究。極渦の可視化に必須 | P1 |
| Lambert Conformal Conic | 中緯度の局所拡大 | P2 |

### 4.4 ベクトル場の重ね合わせ

スカラーフィールド上にベクトル場（風向風速等）を矢印またはストリームラインで重ね描き。
spmodel-rs は `u_cos`, `v_cos`（cos phi で重み付けされた速度場）を出力する。

| 描画法 | 説明 | 優先度 |
|--------|------|--------|
| Wind arrows | 等間隔の矢印。向き=風向、長さ=風速 | P0 |
| Streamlines | 流線。流れの全体像を把握 | P1 |
| Wind barbs | 気象学標準の記法 | P2 |

### 4.5 Inspector パネル

GUI で調整可能な描画パラメータ:

- 変数選択（ドロップダウン）
- 投影法選択
- カラーマップ選択（プレビュー付き）
- 値範囲（自動 / 手動）
- 等値線レベル（自動 / 手動）
- 時間ステップ / 鉛直レベル
- ポイント情報（カーソル位置の座標・値）

### 4.6 コード双方向同期

**同期方式: GUI マスター方式**

Code Panel は「現在の GUI 状態のスナップショット」を常に反映する。
コードと GUI が矛盾する問題を構造的に回避する。

- GUI 操作 → Code Panel が自動更新（デバウンス: 操作静止 0.5秒後に確定）
- ユーザーがコードを編集中 → 自動更新を一時停止
- 「実行」ボタン押下 → コードが GUI 状態を上書き
- 球面回転中は `center=(lon, lat)` の値のみ連続更新し、操作完了後に1行として確定

**GUI → コード**:
- すべての GUI 操作が意味レベルのコードとして Code Panel に表示される
- 生成コードは Python (xarray / matplotlib 互換) を第一候補とする

```python
# GeoScope auto-generated
import geoscope as gs

ds = gs.open("exp01.nc")
fig = ds.vorticity.plot(
    time="48h",
    projection="orthographic",
    center=(135, 35),
    cmap="RdBu_r",
    symmetric=True,
)
fig.save("vorticity_48h.png")
```

**コード → GUI**:
- Code Panel でコードを編集 →「実行」ボタンで Viewport に反映
- v0.2 では Python テキスト生成のみ（実行は外部 Python で行う前提）
- 将来的に Rhai / PyO3 による組み込み実行を追加

**レシピ保存**:
- 操作をスクリプトファイル (.py) として保存
- `geoscope run recipe.py` で同じ図を再現
- 教員が学生に配布するユースケースを想定

### 4.7 Progressive Disclosure（段階的 UI 開示）

**Level 0 — 初回起動**:
- ウェルカム画面: ファイルドロップエリア + サンプルデータで試すボタン
- Data Browser + Viewport のみ表示

**Level 1 — 基本操作**:
- Data Browser + Viewport + Time Slider
- Inspector は折りたたみ（右端にティーザータブ「設定」が見えている）
- Code Panel は非表示（下端にプレビュー1行 `>>> # コードを見る` が見えている）

**Level 2 — 探索モード**:
- Inspector を展開 → 投影法、カラーマップなどを調整
- 比較モード ON → パネル分割

**Level 3 — コードモード**:
- Code Panel を展開 → GUI 操作がコードとして表示
- コード編集 → 描画に反映
- スクリプトとして保存

**遷移トリガー**:

| 遷移 | トリガー | 方式 |
|------|---------|------|
| L0 → L1 | ファイルを開いた瞬間 | **自動** |
| L1 → L2 | Inspector 折りたたみクリック / カラーマップ変更試行 | **手動（ナッジあり）** |
| L2 → L3 | Code Panel 展開 / Cmd+K コマンドパレット | **手動** |
| (Copilot) | Copilot タブクリック / Cmd+K (パレット) | **手動 (L2+)** |

**ナッジ（誘導）の手法**:
- パネル端のティーザー: 折りたたみ時も右端/下端にタブが見えている
- コンテキスト駆動ヒント: 同じ操作を3回繰り返す → 「コードで自動化できます」
- **ポップアップ/モーダルは使わない** — 研究者の集中を妨げない

### 4.8 データブラウザ

- 開いたファイルの変数をツリー表示
- 変数のメタデータ（単位、次元、サイズ）をツールチップ表示
- 変数をダブルクリック → 即座に描画
- 複数ファイルを同時に開ける

### 4.9 LLM Copilot

**概要**: LLM による対話的支援。可視化の物理的意味の解説、自然言語による操作指示、
データに基づく分析提案をインタラクティブに行う。

**インタラクションモデル（優先順）**:

| 優先度 | UI | 説明 |
|--------|-----|------|
| **P0** | チャットサイドバー | Code Panel とタブ同居。対話的な質問・解説 |
| **P0** | コマンドパレット | Cmd+K で呼び出し。短い命令を即実行 |
| P2 | コンテキストメニュー | 右クリック→「この構造を説明」等。将来プラグイン |
| P2 | インライン注釈 | ビューポート上のフローティング解説。将来プラグイン |

**チャットパネル配置**: Code Panel とタブ切替で同居

```
[Code] [Copilot]    ← タブで切替
```

- L2 で Code タブの隣に Copilot タブが出現（Progressive Disclosure と整合）
- Copilot の出力が Rhai コードの場合、Code タブに「このコードを実行しますか？」と流し込む
- 会話履歴はセッション内で保持

**コマンドパレット**: Cmd+K で起動。チャットとは別の入口。

```
> explain          → 現在のビューの物理的解説（チャットに回答）
> hovmoller 35N    → Rhai コード生成 → Code Panel → 確認 → 実行
> compare vor div  → 分割ビュー提案 → 確認 → 実行
> anomaly          → 偏差計算の Rhai コード生成
```

パレット = 短い命令、チャット = 対話的議論。

**出力経路**:

```
自然言語入力 → LLM → Rhai コード生成 → Code Panel に表示
                                          ↓
                              ユーザー確認 → 「実行」ボタン → GUI 反映
```

- LLM が生成したコードをユーザーが**確認してから実行**する（安全性）
- コードが**学習教材**にもなる（「ああ、Hovmoller はこう書くのか」）
- GUI Master モード (4.6) との整合性を維持

**LLM 実行方式: ハイブリッド**

| | クラウド API | ローカル LLM |
|---|---|---|
| 用途 | チャット（解説・対話） | パレット（意図→コマンド変換） |
| 品質 | 高 (Claude 等) | 7B-13B で十分 |
| レイテンシ | 0.5-3s | 即座 |
| オフライン | 不可 | **可能** |

- ユーザー設定で切替可能（クラウドのみ / ローカルのみ / ハイブリッド）
- API キーは Settings で設定。初回は未設定でも動く（ローカル LLM フォールバック）

**プライバシー設計**: 生データは送信しない

```
LLM に送信するコンテキスト:
{
  "variable": "air_temperature",
  "dims": {"time": 120, "level": 20, "lat": 64, "lon": 128},
  "current_view": {"type": "globe", "time_index": 60, "level": 0.5},
  "stats": {"mean": 254.2, "std": 28.1, "min": 195.3, "max": 312.8},
  "spatial_pattern": "zonal_band_structure_with_wave4_perturbation",
  "inference_result": {"type": "temperature", "confidence": "high", "source": "CF standard_name"}
}
```

**コンテキスト階層**:

| Layer | 内容 | 送信先 |
|-------|------|--------|
| Layer 0: ドメイン知識 | GFD 基礎概念・方程式体系・典型パターン辞書 + Rhai API 仕様 | System Prompt (ローカル保持) |
| Layer 1: セッション | ファイルメタデータ、変数一覧、3-stage fallback 推定結果 | ファイル読み込み時に構築 |
| Layer 2: ビューステート | 表示変数・時刻・レベル・カメラ位置・可視範囲の統計量・空間パターン特徴量 | 質問時にオンデマンド計算 |
| Layer 3: 会話履歴 | 直近の対話コンテキスト | チャットセッション内 |

**Layer 0 ドメイン知識の範囲**:

- GFD 基礎: スペクトル法、渦度-発散形式、浅水方程式、プリミティブ方程式
- 典型パターン辞書: ロスビー波、ハドレー循環、極渦、ITCZ、ジェット気流、バロクリニック不安定
- 方程式体系: ispack-rs / spmodel-rs の数学的規約（正規化、座標系）
- Rhai API: GeoScope のスクリプト API 全仕様
- 自由に拡張可能: ドメイン知識ファイル (.md / .toml) をユーザーが追加できる仕組み

**3つの機能モード**:

| モード | 説明 | 例 |
|--------|------|-----|
| **Explain** | 現在のビューの物理的意味を解説 | 「この渦度パターンは中緯度のロスビー波 波数4 です」 |
| **Explore** | 自然言語 → 可視化操作 | 「ロスビー波の伝播を見たい」→ Hovmoller に切替 |
| **Suggest** | データから分析提案 | 「Day 45 で急激な温度変化。突然昇温イベントの可能性」 |

MVP (v0.2) では Explain + Explore。Suggest は v0.3。

---

## 5. 画面構成

```
+----------+--------------------------------------+---------------+
|          |                                      |               |
| Data     |         Main Viewport                |  Inspector    |
| Browser  |                                      |               |
|          |    +-------------------------+       |  Variable:    |
| [exp01]  |    |                         |       |  [vorticity]  |
|  |- vor  |    |      Globe View         |       |               |
|  |- div  |    |                         |       |  Projection:  |
|  |- temp |    |   (drag to rotate)      |       |  [Orthographic|
|  +- ps   |    |                         |       |            ]  |
| [exp02]  |    |                         |       |               |
|  |- vor  |    +-------------------------+       |  Colormap:    |
|  |- ...  |                                      |  [RdBu_r    ] |
|          |  <=========O==================> > || |               |
|          |  t=0       t=48h          t=720h     |  Level:       |
|          |                                      |  [auto      ] |
|          |  [Globe][Map][Hov][Spec][Sect][Prof]  |               |
|          |                                      |  Range:       |
+----------+--------------------------------------+  [-5e-5,5e-5] |
|                                                  |               |
|  >>> # Auto-generated code                       |  Point Info:  |
|  >>> ds = open("exp01.nc")                       |  lon=135E     |
|  >>> ds.vor.isel(time=48).plot.globe(            |  lat= 35N     |
|  ...     cmap="RdBu_r")                         |  val= 2.3e-5  |
|  >>>                                             |               |
|  [Code] [Copilot] [Console] [History]             |               |
+--------------------------------------------------+---------------+
```

### パネル一覧

| パネル | 位置 | 役割 | 初期状態 |
|--------|------|------|---------|
| Data Browser | 左 | ファイル・変数ツリー | 表示 |
| Main Viewport | 中央 | メイン描画領域 | 表示 |
| Inspector | 右 | 描画設定の調整 | 折りたたみ |
| Time Slider | 中央下 | 時間ステップ制御 | データに時間軸があれば表示 |
| View Tabs | 中央下 | ビュータイプ切替 | 表示 |
| Code Panel | 下 | 自動生成コード / コンソール | 非表示 |
| Copilot | 下 (Code と同居) | LLM チャット / コマンドパレット | 非表示 (L2 で出現) |

### レスポンシブ対応（小画面・13インチノートPC）

| 画面幅 | 対応 |
|--------|------|
| 1440px+ | 全パネル通常表示 |
| 1280-1440px | Inspector を Viewport オーバーレイ（フローティング） |
| 1024-1280px | Data Browser もオーバーレイに切替 |
| 1024px 以下 | タブ切り替え方式（Data Browser / Viewport / Inspector を排他表示） |

- Viewport ダブルクリックで最大化（教員プレゼン時に有用）、再度ダブルクリックで復帰

---

## 6. 既存ツールとの差別化

v0.1b 時点での現実的な評価（◎=卓越 ○=良好 △=限定的 ✕=非対応）:

|                     | GrADS | Panoply | ParaView | Matplotlib | **GeoScope v0.1b** | **GeoScope 将来** |
|---------------------|:-----:|:-------:|:--------:|:----------:|:------------------:|:-----------------:|
| Zero-config 描画    |   △   |    ○    |    ✕     |     ✕      |       **○**        |      **◎**       |
| 球面直接操作        |   ✕   |    △    |    ○     |     ✕      |       **○**        |      **◎**       |
| 探索的解析          |   △   |    △    |    ○     |     △      |         △          |      **◎**       |
| コード生成          |   ✕   |    ✕    |    △     |     -      |         ✕          |      **◎**       |
| Publication 品質    |   ○   |    △    |    ○     |     ◎      |         △          |        ○         |
| 学習コスト(低=良)    |   ✕   |    ○    |    △     |     △      |       **◎**        |      **◎**       |
| GFD ドメイン特化    |   ○   |    △    |    ✕     |     △      |       **○**        |      **◎**       |
| スペクトル法連携    |   ✕   |    ✕    |    ✕     |     △      |       **○**        |      **◎**       |
| LLM 支援           |   ✕   |    ✕    |    ✕     |     ✕      |         ✕          |      **◎**       |

**持続的な差別化ポイント** (Python エコシステムに追いつかれにくい領域):
1. dcmodel エコシステムとの深い統合（スペクトル空間 ↔ 格子点空間の対応表示）
2. 「Teach, don't hide」の実装品質（ドメイン知識の深さで差がつく）
3. 単一バイナリ配布（conda/pip 地獄からの解放）
4. ネイティブ起動速度（授業ライブデモでの実感差）
5. **LLM Copilot**: GFD ドメイン知識を持つ対話的支援（他ツールに類例なし）

---

## 7. 技術スタック

| レイヤー | 技術選定 | 選定理由 |
|---------|---------|---------|
| GUI フレームワーク | **egui + egui_dock** | ドッキング UI 成熟、wgpu 公式統合。Iced はドッキング不在で不採用 |
| 3D レンダリング | **wgpu** | WebGPU ベース、将来ブラウザ移植可能。60fps 余裕 |
| NetCDF 読み込み | **netcdf-rs** (v0.12.0) | georust 傘下でアクティブメンテ。libnetcdf の C 依存あり |
| 地図投影 | 自前実装 | ispack-rs の球面座標変換知見を活用 |
| コード生成 | **Python テキスト生成** | xarray / matplotlib 互換コードを文字列として出力（PyO3 不要） |
| スクリプトエンジン (MVP) | **Rhai** | 軽量・安全・配布問題なし。バイナリ組み込み |
| スクリプトエンジン (将来) | **PyO3** | 研究者に馴染む Python 環境。配布コストが課題なので後回し |
| LLM クラウド | **Anthropic API (Claude)** 等 | 高品質な解説・対話。API キーをユーザー設定 |
| LLM ローカル | **llama.cpp / candle** (検討中) | オフライン対応。パレットの意図→コマンド変換に 7B-13B |
| ドメイン知識管理 | **.md / .toml ファイル** | System Prompt 構築。ユーザー拡張可能 |

### Globe 描画アーキテクチャ

```
[NetCDF data] → [CPU: 2D array] → [GPU: 2D Texture (lon x lat)]
                                          |
[Colormap LUT] → [GPU: 1D Texture] → [Fragment Shader: lookup]
                                          |
                                   [Sphere Mesh → Screen]
```

- カラーマップ変更 = 1D テクスチャ差し替えのみ
- データ更新 = 2D テクスチャ転送のみ
- fludarium の既存レンダラーを移植・再利用

### ピボット判断

v0.1a 完了時点 (2ヶ月) で egui + wgpu の統合品質を評価する。
Inspector / Data Browser の実装が困難な場合、**Tauri (Web フロント)** へのピボットを検討する。

**将来のブラウザ対応**: wgpu は WebAssembly + WebGPU へのコンパイルが可能。
デスクトップファーストで設計しつつ、将来のブラウザ版への道を閉じない。

---

## 8. スコープ定義

### v0.1a — 技術検証 (1-2ヶ月)

目標: 「spmodel-rs の NetCDF 出力を D&D で Globe 表示できる」

- [ ] wgpu + egui (egui_dock) のスケルトン UI
- [ ] NetCDF ファイル D&D → Globe ビュー（回転・ズーム）
- [ ] fludarium レンダラーの移植（テクスチャ方式）
- [ ] 最小限のカラーマップ (viridis, RdBu_r)
- [ ] **ピボット判断**: egui + wgpu の統合品質を評価。問題あれば Tauri へ

### v0.1b — 使える最小形 (+2-3ヶ月)

目標: 「Panoply より快適に dcmodel 出力を見られる」

- [ ] Data Browser（変数ツリー）
- [ ] Inspector（投影法、カラーマップ、値範囲）
- [ ] タイムスライダー
- [ ] Map ビュー（Equirectangular デフォルト、Mollweide）
- [ ] 自動推論（3段階フォールバック）
- [ ] ホフメラー図
- [ ] E(n) エネルギースペクトル図（1D log-log）
- [ ] ベクトル場（矢印）重ね合わせ

### v0.2

- [ ] Code Panel（GUI → Python コード生成）
- [ ] コード → GUI 反映（GUI マスター方式）
- [ ] レシピ保存・読み込み
- [ ] 断面図ビュー（Zonal Mean トグル付き）
- [ ] プロファイルビュー
- [ ] 波数フィルタスライダー（スペクトル法教育機能）
- [ ] Polar Stereographic 投影
- [ ] ストリームライン描画
- [ ] **LLM Copilot: チャットパネル** (Explain + Explore モード)
- [ ] **LLM Copilot: コマンドパレット** (Cmd+K → 自然言語 → Rhai)
- [ ] **LLM Copilot: ハイブリッド実行** (クラウド API + ローカル LLM 選択)
- [ ] **LLM Copilot: コンテキスト構築** (Layer 0-2, メタデータのみ送信)
- [ ] **LLM Copilot: ドメイン知識** (GFD 基礎 + スペクトル法 + Rhai API)

### v0.3

- [ ] 比較モード（並列ビュー、差分ビュー、同期回転・ズーム）
- [ ] 複数ファイルの同時閲覧
- [ ] アニメーション GIF / MP4 出力
- [ ] Publication-quality 図のエクスポート
- [ ] 保存量診断ミニパネル（質量・エネルギー・エンストロフィー）
- [ ] 質量流線関数の計算・表示
- [ ] **LLM Copilot: Suggest モード** (データ駆動の自動分析提案)
- [ ] **LLM Copilot: コンテキストメニュー統合** (右クリック→「この構造を説明」)
- [ ] **LLM Copilot: インライン注釈** (ビューポート上の物理解説オーバーレイ)

### 将来

- [ ] ブラウザ版 (WebAssembly + WebGPU)
- [ ] サーバーサイドレンダリング（大規模データ対応）
- [ ] プラグインシステム
- [ ] EP フラックス断面図
- [ ] 球面調和関数モード表示（Y^m_n の個別パターン表示、教育用）
- [ ] 波数-周波数スペクトル

---

## 9. 非機能要件

| 項目 | 要件 |
|------|------|
| 初期描画速度 | ファイルオープンから描画まで 1 秒以内（100MB 以下のファイル） |
| フレームレート | 球面回転操作中 60fps |
| メモリ使用量 | 1GB 以下のファイルはメモリ内で処理 |
| 対応 OS | macOS, Linux（Windows は将来） |
| アクセシビリティ | キーボード操作のみで基本機能が利用可能 |
| 多言語対応 | 日本語 / 英語（UI ラベル） |

---

## 10. 成功指標

### 定量指標（自動測定可能）

| 指標 | 測定方法 | 目標 |
|------|---------|------|
| 初回描画到達時間 | アプリ内タイマー | 30 秒以内 |
| 平均セッション時間 | opt-in テレメトリ | 5分以上（使い捨てでない） |
| GitHub Stars | GitHub | 6ヶ月で 100+ |
| ダウンロード数 | Releases / Homebrew | 月間 50+ |

### 定性指標（手動測定）

| 指標 | 測定方法 | 目標 |
|------|---------|------|
| 電脳倶楽部セミナー後の反応 | アンケート | 「使いたい」70%+ |
| 研究室導入数 | 手動カウント | 初年度 3研究室+ |

---

## 11. 次のステップ

### デザインフェーズ（現在）

1. **Pencil でインタラクティブモックアップを作成**
   - Level 0: ウェルカム画面
   - Level 1: 基本操作画面（Globe ビュー + Data Browser）
   - Level 2: 探索画面（Inspector 展開）
   - Level 3: コードモード画面
   - 13インチノートPC でのレスポンシブ表示
2. ペルソナごとのユーザーフロー検証

### 開発フェーズ

3. **v0.1a**: 技術 PoC（wgpu + egui + egui_dock で Globe 描画）— 1-2ヶ月
4. **ピボット判断**: egui 統合品質の評価
5. **v0.1b**: 使える最小形 — +2-3ヶ月
6. サンプルデータの準備（spmodel-rs Rossby-Haurwitz / Held-Suarez 出力 NetCDF）

### コミュニティ導入

7. **Phase 0** (今): 電脳倶楽部主要メンバーに PRD を見せて早期フィードバック
8. **Phase 1** (v0.1a): spmodel-rs 出力を GeoScope で開くデモ動画
9. **Phase 2** (v0.1b): 電脳倶楽部セミナーでライブデモ
10. **Phase 3** (v0.2): 大学院講義でパイロット導入（1-2研究室）

### 並行タスク: gtool-rs の改善

- フィールド変数に `standard_name` を自動設定する仕組みを追加
  (`vort` → `atmosphere_relative_vorticity`, `phi` → `geopotential` 等)
- GeoScope のフォールバック推論とセットで進める

---

## 付録 A: 既存ツールの課題分析

### A.1 「コードを書かないと何も見えない」問題

GrADS, NCL, Matplotlib 系はすべて「まずスクリプトを書く」前提。
データを開いて「ざっと見る」だけで 10〜20 行のコードが必要。
地図投影の設定、カラーマップ、等値線レベルなど「お約束」の設定が毎回必要。
探索的解析のフリクションが極めて高い。

### A.2 「時代が止まった GUI」

Panoply, GrADS の GUI は 2000 年代の Java Swing / Tcl/Tk 感覚。
高 DPI ディスプレイ非対応、D&D なし、アンドゥなし、ダークモードなし。

### A.3 「球面データ特有の操作」が貧弱

地球を回転させながらデータを見る操作が直感的でない。
断面図の切り替えが手間。アニメーションのシークバーがない or 貧弱。
スペクトル空間と格子点空間の行き来を可視化できるツールがほぼない。

### A.4 「大規模データへのスケーラビリティ」

T213 (640x320) 程度でも時系列を持つと数十 GB。
全球雲解像モデル（数 km 格子）は TB 級。メモリ不足で固まる。

### A.5 「比較」が難しい

2 つの実験結果を並べて見るのが面倒。差分のインタラクティブ表示がない。
観測データとモデル出力の overlay が困難。

### A.6 「共有・再現性」の断絶

可視化の状態を保存・共有する仕組みがない。
論文用の publication-quality 図とインタラクティブ探索が完全に別ワークフロー。

### A.7 学習曲線の壁

各ツールの独自スクリプト言語の習得コストが高い。
「先輩のスクリプトをコピーして改変」文化による理解なき利用。

---

## 付録 B: 参考ツール一覧

### 研究コミュニティの定番

- **GrADS**: COLA/IGES, 1988年〜。CLI + スクリプト
- **NCL**: NCAR, 2019年メンテナンスモード。スクリプト言語
- **ParaView**: Kitware/Sandia。GUI (Qt)、CFD 全般
- **Panoply**: NASA GISS。GUI (Java/Swing)、NetCDF/HDF 簡易閲覧
- **VAPOR**: NCAR。GUI (Qt + OpenGL)、3D 大気・海洋データ
- **Ferret / PyFerret**: NOAA PMEL。CLI + スクリプト、海洋データ

### Python エコシステム

- **Matplotlib + Cartopy**: 2D 地図投影の定番
- **xarray + hvPlot**: NetCDF 直接扱い + インタラクティブ
- **Plotly / Dash**: Web ベースダッシュボード
- **PyVista**: 3D 可視化 (VTK ラッパー)
- **UXarray**: 非構造格子データ向け

### Web ベース

- **Cesium**: 3D 地球儀 WebGL
- **deck.gl**: 大規模地理空間可視化
- **NASA Worldview**: 衛星データ Web 閲覧

### GFD 電脳倶楽部系

- **gtool**: 独自バイナリフォーマット + 可視化
- **DCL**: Fortran 描画ライブラリ
- **gpview / figgen**: DCL ベースの簡易可視化コマンド
